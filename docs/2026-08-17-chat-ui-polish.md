# 2026-08-17 — Chat UI polish

## What

Refreshed the Chat tab for item / library / selection scopes:

- Centered readable column (max ~720pt) instead of full-width walls of text
- Assistant bubbles with avatar, markdown text, constrained width
- User bubbles constrained and right-aligned
- Thinking state as a proper bubble
- Empty state with one-tap prompt chips
- Composer: multi-line field, ⌘↩ send, bar chrome
- Auto-scroll to latest message

## Files

- `Sources/grist/ChatView.swift`
